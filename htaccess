


# Ativa o mecanismo de reescrita de URLs
RewriteEngine ON


# Verifica se o arquivo solicitado não existe como arquivo regular
RewriteCond %{REQUEST_FILENAME} !-f


# Verifica se o arquivo solicitado não existe como diretório
RewriteCond %{REQUEST_FILENAME} !-d


# Redireciona todas as requisições para index.php passando o caminho como parâmetro 'router'
RewriteRule ^(.*)$ index.php?router=$1 [QSA,L]